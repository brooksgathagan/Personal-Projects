using System;
using System.Collections.Generic;
using System.Text;

namespace DefenseGame
{
    /*
    class SomethingMore : Something
    {
        int y = 15;

        public SomethingMore(int abc) { }

        // Used to demonstrate constructors because apparently Treehouse didn't explain them very well.  We can do this -- we don't NEED to call ": base(abc)" here.  To do this, we must define a default constructor in Something.cs.  
        // We must define the default constructor because we defined a non-default  constructor.  If we had no constructor at all, this code would be no problem because the default constructor works (we just have to define it ourselves in order to work if we define other constructors)
    }
    */
    
}

